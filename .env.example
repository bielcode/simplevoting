# Copie para .env e ajuste conforme seu ambiente.
# Nunca versione o .env — ele está no .gitignore.

DB_NAME=drupal10
DB_USER=drupal10
DB_PASSWORD=drupal10
DB_HOST=database
DB_PORT=3306
DB_DRIVER=mysql

HASH_SALT=lando-local-dev
